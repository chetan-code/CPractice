//2.4 - Program to illustrate sizeof() operator
#include <stdio.h>

int main(){
    int i = 0;
    printf("integer : %d \n", sizeof(i));//return number of bytes occupied by operand(argument to the function) - integer here
    return 0;
}